def cap_text(text):
    #the capitalize function just capitalize the first letter in a stringd. if we want to capitalize first letter in each word
    # we need to change from     return text.capitalize() to     return text.capitalize()
    return text.title()

    # return text.capitalize()
